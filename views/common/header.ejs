<head>
    <div class="header">
        <div class="header-wrapper">
            <div class="header-left">
                <a class="header-logo" href="/"><img src="/static/images/building_logo.svg"></a>
            </div>
            <div class="header-right">
                <% if (locals.user){ %>
                    <% if (locals.user.role === '社員') { %>
                    <div>
                        <a class="btn-header" href="/admin">マスタ管理</a>
                    </div>
                    <% } %>
                    <div>
                        <form method="POST" action="/logout">
                            <input class="btn-header" type="submit" button value="ログアウト" />
                        </form>
                    </div>
                <% } %>
                <a class="btn-header" href="javascript:history.back();">戻る</a>
            </div>
        </div>
        <!-- /.header-wrapper -->
    </div>
    <!-- /.header -->
</head>